// function findMultiples(integer, limit) {
//   let multiples = [];
//   for (let i = 1; i <= limit; i++) {
//     let current = integer*i;
//     if (current <= limit) {
//       multiples.push(current); // dodaje element i na koniec tabeli multiples
//     }
//   }
//   return multiples;
// }

findMultiples2 = (integer, limit) => 
console.log(findMultiples(2, 10))
