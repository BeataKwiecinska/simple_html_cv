let caddy = {
    doors: 5,
    color: 'white',
    disel: true
}
let shopinglist = ['milk', 'bread', 'butter'];
// console.log('is it disel?: ', caddy.disel);
// console.log(shopinglist[2]);

for (let i = 0; i<shopinglist.length; i++) {
    console.log(shopinglist[i]);
}
console.log( shopinglist[1][1]);
